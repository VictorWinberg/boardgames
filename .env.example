# Dev server: app is served at the site root.
VITE_BASE_URL=/

# Used only by `npm run fetch:bgg` (not bundled into the app).
BGG_USERNAME=

# Create an application at https://boardgamegeek.com/applications and paste the access token.
BGG_ACCESS_TOKEN=
